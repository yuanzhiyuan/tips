1、避免内存泄露，用智能指针
auto_ptr：不能多次引用
shared_ptr 引用计数

new 之后记得delete，之后指针置0



2、离开作用域之后，只对对象本身销毁，不会销毁指针。如:
	{
		item i;	
		item* j;
	}
会对i析构，但是不对j析构，j指向的对象未销毁，内存泄露。

3、对容器进行销毁时，容器中的元素自动析构。但是，如vector<item*>这种情况，items指针被删除，但是每个item元素没有销毁，内存泄露